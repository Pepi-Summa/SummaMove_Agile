<UserControl x:Class="SummaMove.NavigationMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="70"
             d:DesignWidth="390">

    <Border Background="{StaticResource HeaderGray}"
            Height="70">

        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- CHALLENGE -->
            <Button Grid.Column="0"
                    Content="🎯"
                    FontSize="22"
                    Background="Transparent"
                    BorderThickness="0"                    
                    Foreground="{StaticResource PrimaryBlue}"
                    Cursor="Hand"
                    Click="ChallengeButton_Click"/>

            <!-- TRASH -->
            <Button Grid.Column="1"
                    Content="🗑"
                    FontSize="22"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="{StaticResource PrimaryBlue}"
                    Cursor="Hand"/>

            <!-- HOME -->
            <Button Grid.Column="2"
                    Content="🏠"
                    FontSize="22"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="{StaticResource PrimaryBlue}"
                    Cursor="Hand"
                    Click="HomeButton_Click"/>

            <!-- STATS -->
            <Button Grid.Column="3"
                    Content="📊"
                    FontSize="22"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="{StaticResource PrimaryBlue}"
                    Cursor="Hand"/>

            <!-- PROFILE -->
            <Button Grid.Column="4"
                    Content="👤"
                    FontSize="22"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="{StaticResource PrimaryBlue}"
                    Cursor="Hand"
                    Click="ProfielButton_Click"/>

        </Grid>

    </Border>

</UserControl>
